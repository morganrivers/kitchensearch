# Generated diagram artifacts — only the .json sources and .py generators are
# committed here; the .drawio / .svg / .png are rebuilt from them (locally and
# by the graphviz-diagram CI workflow).
*.drawio
*.svg
*.png
